// 이창근의 C프로그래밍, 1만 시간의 법칙!!
// C언어 코딩 :: chapter 35, 정수 맞교환

#include<stdio.h>

int main(void)

{
	
	int a = 33, b = 77;
	int tmp;
	
	puts("--------------------");
	puts(" ** 정수 맞 교환 ** ");
	puts("--------------------");
	
	printf(" 원래 값 a = %d, b = %d \n", a, b);
	puts("--------------------");
	
	tmp = a;
	a = b;
	b = tmp;
	
	printf(" \n 맞 교환 후 a = %d, b =%d \n", a, b);
	puts("-------------------------------------");
	
	puts(" 값을 맞교환 시 반드시 빈그릇 필요");
	puts("-------------------------------------");
	
	/* 빈 그릇이란, 콜라가 있고, 우유가 있으면 
	서로 맞교환하려면 '빈 그릇'이 하나가 있어야하는 것처럼
	tmp가 빈 그릇이 되어 
	Line 17 tmp = a; 를 하면 tmp = 33이 되고,
	Line 18 a = b; 를 하면 a = 77이 되고, 
	Line 19 b = tmp;를 하면 Line 17에서 tmp = 33이 되었으니,
	결과로는 a = b 가 맞교환이 되는 과정임. */
 
	
	return 0;
	 
		
}
